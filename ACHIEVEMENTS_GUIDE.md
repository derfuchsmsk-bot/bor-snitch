# Гайд по ручному добавлению достижений (Ачивок)

Этот документ описывает, как администратор может вручную выдать достижение пользователю через Firebase Console.

## 1. Доступ к Базе Данных
1. Откройте [Firebase Console](https://console.firebase.google.com/).
2. Выберите ваш проект (`bor-snitch` или аналогичный).
3. В левом меню выберите **Firestore Database**.

## 2. Поиск Пользователя
Данные пользователей хранятся по следующему пути:
`chats` -> `{ID чата}` -> `user_stats` -> `{ID пользователя}`

1. В коллекции `chats` найдите документ, соответствующий ID вашего Telegram-чата (например, `-100123456789`).
2. Внутри этого документа откройте подколлекцию `user_stats`.
3. Найдите документ с ID пользователя (это числовой ID Telegram, например `12345678`).

## 3. Добавление Достижения
1. В документе пользователя нажмите **Add field** (Добавить поле).
2. В поле **Field path** введите: `achievements`.
3. В поле **Type** выберите: `Array`.
4. Теперь вы можете добавлять элементы в этот массив.

### Вариант А: Простое текстовое достижение
Если вам нужна просто надпись.
1. Нажмите **Add element** (или плюсик).
2. Type: `String`.
3. Value: `Ветеран Войны Клоунов`.

### Вариант Б: Красивое достижение с иконкой (Рекомендуется)
Если вы хотите иконку, заголовок и описание.
1. Нажмите **Add element**.
2. Измените Type с `String` на `Map`.
3. Внутри Map добавьте следующие поля:
    *   `icon` (String): Эмодзи, например `🤡`.
    *   `title` (String): Название, например `Король Шутов`.
    *   `description` (String): Описание, например `Получил 50 штрафов за клоуна`.

## 4. Результат
После сохранения изменений, пользователь увидит свои достижения при вызове команды `/status` (или `/me`).

Пример отображения в боте:
> 👤 **Личное Дело: Иван**
> ...
>
> 🏅 **Достижения:**
> 🤡 **Король Шутов** — *Получил 50 штрафов за клоуна*
> • Ветеран Войны Клоунов
